#include <bits/stdc++.h>
using namespace std;

string s;
const int N = 1e5+7;
long long int dp[N];

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(nullptr);
  cout.tie(nullptr);

  int a;
  cin >> a;
  cin >> s;
  for(char i: s) {
    if(i == 'L') {
      
    }
  }


  return 0;
}