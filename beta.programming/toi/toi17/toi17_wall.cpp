#include <bits/stdc++.h>
using namespace std;
typedef long long int lli;
typedef pair<int, int> pii;
typedef pair<short, short> pss;
#define f first
#define s second

#define N 1e3+2
bool house[N][N], visited[N][N], visited2[N][N];

struct coord {
    int x, y;
};
queue<corrd> q;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int m, n;
    cin >> m >> n;


    return 0;
}
