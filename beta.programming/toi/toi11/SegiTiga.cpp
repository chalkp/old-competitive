#include <bits/stdc++.h>
using namespace std;

int n, t=20;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(nullptr);
  cout.tie(nullptr);

  while(t--) {
    cin >> n;
    int segi[n];
    for(int i=0; i<n; i++) {
      
    }
  }

  return 0;
}