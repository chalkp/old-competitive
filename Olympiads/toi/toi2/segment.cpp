#include <bits/stdc++.h>
using namespace std;

const int N = 1e5+7;
long long int n, m;
string line1, line2, line3;

int main() {
  ios_base::sync_with_stdio(false), cin.tie(nullptr), cout.tie(nullptr);
  
  cin >> n >> m;
  for(int i=0; i<3; i++) {
    cin >> line1;
    cin >> line2;
    cin >> line3;
  }
  
  for(int i=0; i<3*n; i++) {
    
  }

  return 0;
}