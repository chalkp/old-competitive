#include "combo.h"
#include <bits/stdc++.h>
#pragma GCC optimize("03, unroll-loops")

std::string guess_sequence(int N) {
  std::string p = "";
  std::string S = "";
  if(press("AB")) {
    if(press("A")) {
      p = ""
      S = "A";
    } else {
      list[2] = false;
      S = "B";
    }
  } else {
    if(press("X")) {
      list[3] = false;
      S = "X";
    } else {
      list = 
    }
  }
  int coins = press(p);
  return S;
}
